## Decisions

1. R-11 必须在 R-01 至 R-10 通过后执行，避免先把未完成项标成完成。
2. 每个 status/checkbox 必须链接到代码文件、测试命令或审计证据。
3. 旧 acceptance report 不删除；修订为当前事实或标注为 superseded，避免保留“无 blocker”的矛盾陈述。
4. 批次源文档的实际路径以 `05_Areas` 为准，并在证据中明确修正路径差异。
