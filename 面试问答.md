# 项目相关
## Mini-Claude-Code
### Q1: 介绍一下你的项目
我之所以会自己做一个 code agent，主要的原因是可控性。平时我也会用 Claude Code 这类产品，但很多关键东西对用户来说其实是黑盒，比如 prompt 是怎么拼的、上下文是怎么组装的、工具在什么条件下会被调用，我很难真正判断它为什么这次做对了、下次又为什么会跑偏，某种程度上有点像抽卡。所以我想自己把这套东西搭起来，把这些关键环节都摊开看清楚。

在设计上我没有把重点放在“接个模型、配点工具”这种 demo 上，而是重点去解决 coding agent 真正落地时最麻烦的几个问题，比如任务怎么稳定执行、长会话怎么不跑偏、高风险操作怎么控住、出了问题之后怎么恢复和复盘。围绕这些点，我做了记忆系统和预算感知的上下文管理、工具审批机制，以及 session 和 trace，最后还配了一套固定任务集和离线 evaluator 去做可复现评测。

从结果看，我的 agent 能够稳定执行任务，在任务集中能够正确地调用工具，能够正确地处理上下文，能够正确地恢复和复盘。项目虽然不大，但执行、治理、恢复和评测这条关键链路已经放进去了。

### Q2: 项目架构
第一层是入口层。
用户在终端输入命令以后，系统先做启动参数解析，比如工作目录、模型后端、执行预算、审批策略这些
这个阶段的目标是把用户的一条命令转换成一个完整的运行上下文。

第二层是上下文构建层。
agent 真正开始推理前，先把当前系统能提供的上下文整理成一个结构化输入。
这里我把上下文分成几种不同类型的数据：
● 稳定上下文，比如工具能力、系统规则、仓库概要
● 会话历史，也就是前面几轮发生过什么
● 当前用户请求
并根据上下文占用阈值情况，判断是否需要压缩旧的上下文。
这里设计的核心思想是，不同寿命的数据不能混成一团。稳定部分应该尽量可复用，例如系统提示词在初始化时就确定了，不会因为记忆或者环境的修改而改变。

第三层是 agent 控制循环，一个ReAct循环。
这部分是整个系统的核心。
它不是一次性把 prompt 发给模型然后等答案，而是一个持续推进的闭环：
1. 构建当前轮上下文  
2. 调模型做决策  
3. 判断模型输出的是最终答案还是动作请求  
4. 如果是动作请求，就去执行工具
5. 把执行结果写回系统状态  
6. 进入下一轮

第四层是执行与记忆更新层。
如果模型决定调用工具，系统不会直接照做，而是先过一层执行网关。
这层主要负责几件事：
● 校验动作是否合法
● 判断是不是重复动作
● 判断是不是高风险动作，需要不要审批
● 真正执行动作
● 把结果转换成系统下一轮可以消费的状态
如果有值得记忆的结果，比如用户反馈、用户偏好，就会被写入记忆系统并更新记忆索引。

第五层是持久化与审计层。
当一轮任务结束以后，系统会把这次运行留下两种不同性质的数据。
第一种是 可恢复状态。
也就是 session 级别的上下文记录，它服务的是“下次还能接着干活”。
第二种是 运行工件。
也就是trace、report 这类单次运行记录，它服务的是“解释这次到底发生了什么”。
它让 agent 系统不只是能跑，还能复盘、调试、评估。
可恢复状态就是 session.json，保存了完整的消息历史和元信息，resume 时直接加载；运行工件是 JSONL trace文件，每轮记录首 token 延迟、token 消耗、工具调用耗时和成功率，方便做性能回归分析。

最后我总结一下
这五层设计分别解决了不同的问题：
● 入口层解决“怎么把命令变成运行现场”
● 上下文构建层解决“怎么把不同寿命的数据组织成模型可消费的输入”
● 控制循环解决“怎么让 agent 持续推进，而不是一轮就结束”
● 执行与记忆更新层解决“怎么安全调用工具，并且把结果沉淀成下一轮还能用的状态”
● 持久化与审计层解决“怎么让系统可恢复、可解释、可评估”

### Q3: 上下文管理怎么做的
上下文管理的核心思想是，把上下文当成一个有预算、有优先级的运行时状态来管理，这块的设计主要解决的问题是本地 coding agent 在多步任务里，怎么让模型每一轮都看到一份够用、稳定、不过载的上下文。
第一层是稳定上下文，也就是system prompt。这部分不是会话历史，而是 agent 每一轮都要用到的固定背景。里面会有它是谁、工具怎么调用、当前仓库的一些稳定事实，比如 Git 状态、最近提交、怎么写记忆、skill描述等等。
第二层就是会话历史，也就是前面几轮发生过什么。这部分是 agent 每一轮都要用到的，但是会话历史的长度是有限的。所以我采用了四层的渐进式压缩策略。
> **第 1 层 — 工具结果轻量截断**: 当上下文占用超过 50% 时，开始截断过长的工具结果，只保留头部和尾部，中间用标识符代替；超过 30KB 的工具结果自动写入本地磁盘，供模型按需检索。
>
> **第 2 层 — 过期结果清理**: 对同一文件的重复读取，只保留最新的读取结果，历史读取结果用占位符代替（`[Old result cleared]`），告诉模型曾经读过但内容已清理。
>
> **第 3 层 — 闲置时间激进清理**: 用户长时间未发送消息时，服务端 kv-cache 已经被清理，此时会做更激进的清理，只保留最新的 3 个工具结果。
>
> **第 4 层 — 全量压缩**: 上下文超过 70% 占用阈值时，启用另一个模型对历史消息进行摘要替换。压缩时排除系统提示词和用户最新一轮消息，通过 prompt 要求模型保留任务决策、文件路径、任务进度等关键信息。

如何评估压缩质量：在长上下文占用的情况下，分别测试开启压缩和关闭压缩的性能，对比任务的成功率和执行工程中工具调用的次数、耗时。

### Q4: Prompt/Context/Harness Engineering
Prompt Engineering解决的问题是怎么把指令说清楚，关注点在让模型理解意图，减少局部歧义，典型的工作是系统提示词设计、Few-shot 示例、思维链引导
Context Engineering解决的问题是该给 Agent 看什么，在合适时机给模型提供正确且必要的信息，例如上下文管理、RAG、记忆注入、Token 优化
Harness Engineering解决的问题是Agent系统怎么持续执行、纠偏、观测和恢复，关注点在长链路任务中的持续正确、偏差修正、故障恢复，文件系统、沙箱、约束执行、反馈回路、观测

### Q5: LangChain和LangGraph对比
LangChain的核心是“链”（Chain），即将多个LLM调用和工具调用链接在一起，形成一个有序的任务序列。其架构遵循有向无环图（DAG）结构，任务按单一方向流动，不形成循环。
适合处理预定义的、顺序明确的工作流程，如数据检索、内容总结、简单问答等。

LangGraph采用图（Graph）结构来表示任务和流程，其中节点代表操作或步骤，边表示节点之间的依赖关系。拥有一个中央状态组件，确保在整个交互过程中保持上下文的连续性，支持循环构建、动态路由，适合构建多个智能体相互协作、或者需要动态、复杂交互、人类介入的工作流程。

### Q6: MCP和CLI对比
MCP（Model Control Protocol）和 CLI（Command Line Interface）都是用于与模型交互的协议，但是它们在设计和使用上有一些关键的区别。
MCP 是一种基于 JSON 的协议，用于与模型进行实时交互。它定义了一组标准的消息格式，包括请求和响应，以及一些元数据字段。MCP 主要用于在模型和客户端之间建立实时的通信渠道，支持模型的动态调用和参数调整。
CLI 是一种基于命令行的协议，用于与模型进行交互。它定义了一组命令，用于执行模型的任务和操作。CLI 主要用于在模型和客户端之间建立一次性的通信渠道，支持模型的批量调用和参数调整。
MCP 与 CLI 的区别主要在于通信方式和交互模式。MCP 是一种实时的协议，用于与模型进行持续的交互，而 CLI 是一种一次性的协议，用于与模型进行批量的交互。
MCP 主要用于在模型和客户端之间建立实时的通信渠道，支持模型的动态调用和参数调整。

### Q7: 你的项目和Claude Code对比？你认为Claude Code最大的优势？
任务管理系统，这使得Claude Code能够更方便地管理和协调多个任务。
1.复杂任务拆解 — 单个 Agent 将大任务分解为可追踪的小步骤
2.进度可见性 — 用户在终端 UI 中实时看到每个步骤的状态
3.多 Agent 协调 — 团队中的多个 Agent 共享任务列表、认领工作、避免重复

这也是我接下来要实现的功能。

### 面试记录
1. 工具管理全链路
2. 为什么说你的agent是coding agent，什么设计决定的？
3. MCP的鉴权机制
4. 怎么沉淀skill，复用知识
5. 上下文摘要的细节，全量摘要要多久
6. 