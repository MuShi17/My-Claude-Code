Metadata-Version: 2.4
Name: rollo-code
Version: 0.1.0
Summary: A local coding agent with a replayable event runtime
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: anthropic>=0.49.0
Requires-Dist: openai>=1.60.0
Requires-Dist: python-dotenv>=1.0.1
Requires-Dist: rich>=13.9.0

# Rollo Code

一个面向编码场景的本地 CLI Agent。它不只封装模型与工具，还尝试解决 Agent 在长任务中的状态一致性、上下文压缩、副作用恢复和运行审计问题。

本项目的核心设计是 **Log is the Runtime**：追加写的 Canonical RuntimeEvent 账本是唯一事实源；模型上下文、会话视图、运行指标和恢复状态都由事件流投影生成，而不是由多份可变状态分别维护。

> 当前版本聚焦 `src/rollo/` 下的 Python 实现，适合学习 Coding Agent Runtime、Context Engineering 与工具安全边界。项目仍处于实验阶段，不应被视为系统级安全沙箱。

## 核心能力

- **双 Provider Runtime**：支持 Anthropic API 与 OpenAI-compatible API，包括流式文本、Thinking 和工具调用。
- **Canonical Event Ledger**：使用 SQLite 按序持久化模型请求、工具生命周期、权限决策、上下文迁移和任务终态。
- **确定性投影与恢复**：由同一事件前缀重建 Provider 上下文、Session、Metrics、Trace 与 Recovery 视图。
- **工具副作用边界**：先持久化 `tool_dispatch`，再执行工具并记录 `tool_outcome`；结果未知时不自动重试。
- **渐进式上下文管理**：支持工具结果归档、过期剪除、空闲微压缩、全量摘要和可验证的 Context Transition。
- **权限与文件安全**：提供 5 种权限模式、allow/deny 规则、危险命令检测和 read-before-edit 文件版本校验。
- **可扩展 Agent Harness**：支持项目规则、Skills、Sub-Agent、MCP stdio 工具和跨会话结构化记忆。
- **评测闭环**：提供 Terminal-Bench 2.1 / Harbor Adapter、任务级预构建镜像映射和结构化 usage 输出。

## Log is the Runtime

传统实现通常把 Provider messages 当作主状态，再额外写 Session、Trace 和 Metrics。这容易产生双写不一致：模型看到的上下文、恢复时加载的历史和审计日志可能不再对应同一次运行。

Rollo Code 将 Canonical Events 作为持久化边界：

```mermaid
flowchart LR
    U[User input] --> A[Agent loop]
    P[Provider stream] --> A
    T[Tool lifecycle] --> A
    A --> E[RuntimeEvent emitter]
    E --> L[(SQLite canonical ledger)]

    L --> MR[Model replay projection]
    MR --> PA[Anthropic / OpenAI adapters]
    PA --> A

    L --> S[Session projection]
    L --> M[Metrics / Trace projection]
    L --> R[Recovery projection]
    L --> C[Compaction checkpoints]
```

这里的 “Log” 不是普通文本日志，而是带有严格 envelope、运行身份、存储顺序和内容摘要的事件账本：

1. **事实与视图分离**：Canonical Event 保存事实；Provider messages、终端展示和指标是可删除、可重建的视图。
2. **冷重放与增量重放一致**：运行中读取事件后缀提高效率，重启后从完整前缀重建，两者必须得到相同上下文。
3. **副作用显式化**：工具调用被拆成 Call、Permission、Dispatch、Outcome；Dispatch 成功落盘后才会执行工具。
4. **压缩不改写历史**：压缩通过带 `source_high_water`、`source_digest` 的 Checkpoint 和 Context Transition 激活新的有效上下文。
5. **运行身份隔离**：Session、Turn、Run、Invocation、Context 与父子关系分别建模，避免子 Agent 或跨轮次串线。

## 快速开始

### 环境要求

- Python 3.11+
- Anthropic API Key，或 OpenAI-compatible API Key 与 Base URL

### 安装

在仓库根目录执行：

```bash
python -m pip install -e ./src
```

开发和运行测试时还需要：

```bash
python -m pip install pytest
```

### 配置 Provider

程序会从当前工作目录自动读取 `.env`。建议只配置一种 Provider，避免配置优先级造成误解。

Anthropic 格式：

```dotenv
ANTHROPIC_API_KEY=your-api-key
# ANTHROPIC_BASE_URL=https://your-anthropic-compatible-endpoint
ROLLO_MODEL=claude-opus-4-6
ROLLO_THINKING_EFFORT=max
```

OpenAI-compatible 格式：

```dotenv
OPENAI_API_KEY=your-api-key
OPENAI_BASE_URL=https://your-openai-compatible-endpoint/v1
ROLLO_MODEL=your-model-name
ROLLO_THINKING_EFFORT=max
```

`ROLLO_THINKING_EFFORT` 可选 `none`、`low`、`high`、`max`，默认值为 `max`。具体 Thinking 能力取决于模型和兼容端点。

### 运行

```bash
# 交互式 REPL
rollo

# One-shot
rollo "定位并修复这个项目中的测试失败"

# 模块入口
python -m rollo "解释当前项目的运行时架构"
```

启动命令所在目录即 workspace。文件工具、Shell、规则、Skills、MCP 和记忆都绑定到这个显式的 ProjectContext。

## CLI

| 参数 | 作用 |
| --- | --- |
| `--model`, `-m` | 覆盖模型名称 |
| `--api-base URL` | 使用 OpenAI-compatible API 地址 |
| `--thinking-effort LEVEL` | 设置 `none` / `low` / `high` / `max` |
| `--thinking` / `--no-thinking` | 显式开启或关闭 Thinking |
| `--resume` | 从最新 Canonical Session 重建上下文 |
| `--list` / `--latest` | 离线列出 Session / 显示最近 Session |
| `--max-cost USD` | 设置估算费用上限 |
| `--max-turns N` | 设置 Agent 轮次上限 |
| `--plan` | 只读计划模式 |
| `--accept-edits` | 自动批准文件编辑，危险 Shell 仍需确认 |
| `--dont-ask` | 自动拒绝所有需要确认的操作，适合 CI |
| `--yolo`, `-y` | 跳过所有确认；仅在可信隔离环境中使用 |

```bash
rollo --plan "设计一个新的缓存模块"
rollo --accept-edits "完成重构并运行相关测试"
rollo --max-cost 0.50 --max-turns 20 "实现 feature X"
rollo --resume
```

REPL 内置命令：

| 命令 | 作用 |
| --- | --- |
| `/clear` | 清空当前有效上下文 |
| `/plan` | 切换计划模式 |
| `/cost` | 显示 Token 与费用统计 |
| `/compact` | 手动触发全量压缩 |
| `/memory` | 列出当前 workspace 的记忆 |
| `/skills` | 列出可用 Skills |
| `/<skill-name> [args]` | 调用用户可调用的 Skill |
| `exit` / `quit` | 退出 REPL |

## Runtime 设计

### Canonical RuntimeEvent

每条事件包含 Session、Turn、Run、Invocation 和 Context 身份，并由 SQLite 在事务中分配顺序。事件覆盖用户输入、模型输出、Thinking、工具调用、权限决策、Dispatch、Outcome、Provider usage、错误、上下文迁移和任务终态。

Canonical Store 采用单逻辑写者和短事务。提交失败时不会在边界层自动重试，因为调用方可能正处于一个无法安全重复的外部副作用附近。

### Provider-neutral replay

`ModelReplayProjection` 先从事件流生成 Provider-neutral 历史，再映射为 Anthropic content blocks 或 OpenAI messages/tool calls。内部运行 ID、投影元数据和诊断信息不会进入 Provider 请求。

运行中使用增量游标读取事件后缀；遇到上下文迁移或一致性异常时回退到冷重建。Provider 请求发送前还会进行最终容量检查，无法安全缩减时直接失败。

### Durable tool boundary

```text
model tool call
    -> permission decision
    -> durable tool_dispatch
    -> execute side effect
    -> durable tool_outcome
```

如果进程在 `tool_dispatch` 与 `tool_outcome` 之间退出，Recovery Projection 会将操作标记为 `uncertain`。系统不会把“没有看到结果”等同于“没有执行”，因此不会自动重放这类操作。

### Context Engineering

- 大型或历史工具结果可归档为不可变 Artifact，并通过受限的 `ArchiveRead` 分页读取；
- 上下文压力升高时，对旧的只读工具结果执行预算裁剪和过期剪除；
- 长时间空闲后清理旧工具结果的有效视图；
- 接近窗口上限时生成摘要，以可验证的 Compaction Checkpoint 原子激活新上下文；
- 记忆系统通过 LLM side query 做语义选择，并在满足门控条件时异步预取。

原始 Canonical 事实不会因为上下文压缩而被就地改写。

### Streaming partials

流式增量属于“尚未完成的观察”，与最终事实分开保存。Runtime 对 partial snapshot/segment 做有界批量持久化；完整文本、工具调用和终态才进入不可变事件流。这样既保留中断诊断证据，也不会让未完成片段污染确定性重放。

## 权限与安全

| 参数 | 内部模式 | 行为 |
| --- | --- | --- |
| 无 | `default` | 只读工具自动放行；危险 Shell 和新建文件需要确认 |
| `--plan` | `plan` | 禁止 Shell，只允许写入计划文件 |
| `--accept-edits` | `acceptEdits` | 自动批准文件编辑，其他危险操作仍确认 |
| `--dont-ask` | `dontAsk` | 自动拒绝需要确认的操作 |
| `--yolo` | `bypassPermissions` | 跳过权限确认 |

可在 `~/.rollo/settings.json` 或 `<workspace>/.rollo/settings.json` 中声明规则；`deny` 优先于 `allow`：

```json
{
  "permissions": {
    "allow": ["read_file", "run_shell(python -m pytest*)"],
    "deny": ["run_shell(git push*)"]
  }
}
```

文件编辑使用 read-before-edit 保护：Agent 必须先读取目标文件；如果文件在读取后又被修改，编辑会被拒绝。

> 权限系统是 Agent 层的操作治理，不是 OS 容器或系统调用级沙箱。`--yolo` 下的 Shell 继承当前用户权限。

## 工具与扩展

### 内置工具

当前 Runtime 提供 13 个内置工具：

- 文件与搜索：`read_file`、`write_file`、`edit_file`、`list_files`、`grep_search`；
- 执行与网络：`run_shell`、`web_fetch`；
- Agent Harness：`agent`、`skill`、`tool_search`、`enter_plan_mode`、`exit_plan_mode`；
- 运行时能力：`ArchiveRead`。

只读且并发安全的工具会在同一轮并行执行；具有副作用的工具经过权限检查后串行执行。低频工具可通过 `tool_search` 延迟激活。

### Skills 与 Sub-Agent

Skills 从以下位置发现，项目级同名 Skill 覆盖用户级：

```text
~/.rollo/skills/<name>/SKILL.md
<workspace>/.rollo/skills/<name>/SKILL.md
```

```markdown
---
name: review
description: Review a change for correctness and regressions
user-invocable: true
context: inline
allowed-tools: read_file, grep_search
---

Review the requested change. Focus on $ARGUMENTS.
```

`context: inline` 将提示词注入当前对话；`context: fork` 在隔离的 Sub-Agent 上下文中执行。内置 Sub-Agent 包括只读搜索型 `explore`、只读规划型 `plan` 和通用型 `general`；也可在 `~/.rollo/agents/*.md` 或 `<workspace>/.rollo/agents/*.md` 中定义自定义类型。

### MCP

Runtime 支持 JSON-RPC over stdio 的 MCP Server。配置可放在项目 `.mcp.json`、项目 `.rollo/settings.json` 或用户级 `~/.rollo/settings.json`：

```json
{
  "mcpServers": {
    "example": {
      "command": "python",
      "args": ["path/to/server.py"],
      "env": {"EXAMPLE_TOKEN": "..."}
    }
  }
}
```

发现的工具以 `mcp__<server>__<tool>` 命名，避免不同 Server 之间发生名称冲突。

## 本地数据

默认数据根目录为 `~/.rollo/`，可通过 `ROLLO_RUNTIME_DIR` 覆盖：

```text
~/.rollo/
├── sessions/<session-id>/
│   ├── runtime.sqlite      # Canonical Event Ledger
│   └── session.v2.json     # 可删除、可重建的 Session 投影缓存
├── artifacts/              # 归档的大型工具结果和运行工件
└── projects/<workspace-id>/memory/
    ├── MEMORY.md           # 自动生成的记忆索引
    └── *.md                # user / feedback / project / reference 记忆
```

`runtime.sqlite` 是恢复的事实源；`session.v2.json` 只是派生快照，损坏或缺失时可以重建。

## 测试

```bash
python -m pytest -q src/rollo/tests
```

测试覆盖 Event schema、SQLite 顺序与完整性、冷/热重放一致性、Provider wire shape、父子上下文隔离、工具副作用恢复、Context Transition、Artifact Archive、权限交互和 CLI smoke 等边界。

## Terminal-Bench 2.1 / Harbor

仓库提供 `benchmark.harbor_agent:RolloHarborAgent`。在 Windows PowerShell 中，从仓库根目录运行：

```powershell
conda activate py313
$env:PYTHONPATH = (Get-Location).Path
$runDir = "benchmark_runs/$(Get-Date -Format 'yyyy-MM-dd__HH-mm-ss')"

& "$env:CONDA_PREFIX\Scripts\harbor.exe" run `
  --agent benchmark.harbor_agent:RolloHarborAgent `
  --model deepseek-v4-flash `
  --dataset terminal-bench/terminal-bench-2-1 `
  --task terminal-bench/vulnerable-secret `
  --jobs-dir $runDir `
  --n-concurrent 1 `
  --env-file .env `
  --yes
```

Harbor 环境需要 Python 3.12+。请在 `.env` 中配置与模型匹配的 Provider、`ROLLO_MODEL` 和 `ROLLO_THINKING_EFFORT`。结果写入 `benchmark_runs/<timestamp>/`；Canonical Runtime 数据会进入 Harbor 挂载的 agent-log 目录，在外层超时后仍可用于诊断。

需要减少任务容器内的重复安装时，可以使用 `benchmark/images/` 中的 Dockerfile 构建派生镜像，并通过 `benchmark/prebuilt-images.json` 与 `benchmark.prebuilt_environment:RolloPrebuiltDockerEnvironment` 按任务选择镜像。

## 项目结构

| 路径 | 职责 |
| --- | --- |
| `src/rollo/agent.py` | Agent 循环、Provider 调用、工具调度和上下文编排 |
| `src/rollo/runtime_event.py` | Canonical RuntimeEvent schema 与校验 |
| `src/rollo/runtime_store.py` | SQLite 账本、顺序、摘要、工具与压缩事务 |
| `src/rollo/runtime_lifecycle.py` | 模型/工具生命周期与 Durable Tool Boundary |
| `src/rollo/projections/` | Model Replay、Provider、Session、Metrics 与 Trace 投影 |
| `src/rollo/recovery.py` | 启动恢复分类与保守关闭策略 |
| `src/rollo/archive_*` | 大型工具结果的归档、读取与 Provider 投影 |
| `src/rollo/compaction.py` | 可验证的 Compaction Checkpoint |
| `src/rollo/context_transition.py` | 有效上下文迁移与激活校验 |
| `src/rollo/tools.py` | 内置工具、权限规则与文件安全检查 |
| `src/rollo/memory.py` | 结构化记忆、索引、语义召回与异步预取 |
| `src/rollo/skills.py` | Skill 发现与 inline/fork 执行 |
| `src/rollo/subagent.py` | 内置与自定义 Sub-Agent |
| `src/rollo/mcp_client.py` | MCP stdio 连接、工具发现和命名空间路由 |
| `benchmark/` | Harbor Adapter 与预构建镜像支持 |

## 当前边界

- Canonical SQLite Store 面向本地单逻辑写者，不是分布式事件总线。
- OpenAI-compatible 端点对 Thinking、usage 与 tool calling 的实现并不完全一致，需要按实际服务验证。
- 工具权限降低误操作风险，但不能替代容器、虚拟机或操作系统级隔离。
- Benchmark 结果受模型版本、任务版本、Thinking、超时与环境镜像影响；对外报告成绩时应同时保留这些配置。
